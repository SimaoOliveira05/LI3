#ifndef TYPE_H
#define TYPE_H

/**
 * @brief Converts an int to a type.
 * 
 * @param type 0 for individual, 1 for group.
 * @return char* Individual or group.
 */
char* convert_type(short int type);

#endif